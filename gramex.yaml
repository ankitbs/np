# Generated by gramex init 1.58.0 (don't delete this line)
# on 2020-04-09 by Vinoth <vinoth.sekaran@gramener.com>


variables:
  # connection_string: '$MYSQL_CONNECTOR://$MYSQL_USER:$MYSQL_PASSWORD@$MYSQL_HOST:$MYSQL_PORT/phase_two'
  connection_string: "$PG_CONNECTOR://$PG_USER:$PG_PASSWORD@$PG_HOST:$PG_PORT/nikshay"
  # connection_string: 'mysql://root:root@localhost:3306'

# Configurations for app: ndpp
# ----------------------------------------------------------------------------
app:
  settings:
    xsrf_cookies: false

url:
  ndpp-patient-enroll:
    pattern: /$YAMLURL/patient/enroll
    handler: FunctionHandler
    kwargs:
      # xsrf_cookies: false
      function: ndpp.enroll_patient(handler)
